<Window x:Class="Dungeon.ConversationDesigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dungeon.ConversationDesigner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.8*" />
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Subjects}"
                 SelectedItem="{Binding Selected}" Margin="0,21,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="18" Text="{Binding Path=Name}" />
                        <TextBlock Text="{Binding Path=Text}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <GridSplitter Width="4"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Stretch"
                      Background="Black" />

        <StackPanel Grid.Column="1" DataContext="{Binding Selected}">
            <TextBlock Text="Выбранный элемент"  />
            <TextBlock Text="Модель" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Производитель" />
            <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />
            <ListBox Grid.Column="0" ItemsSource="{Binding Replics}"
                 SelectedItem="{Binding SelectedReplica}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock FontSize="18" Text="{Binding Path=Answer}" />
                            <TextBlock Text="{Binding Path=Text}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Menu Height="21" VerticalAlignment="Top" Margin="0,0,4,0" >
            <MenuItem Header="File">
                <MenuItem Header="Open" Click="OpenClick" />
                <MenuItem Header="Save" Click="SaveClick" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>
